<!DOCTYPE html>
<html lang="zh-CN" <%= platform_identifier %>>
  <head>
    <title><%= page_title %></title>
    <%= viewport_meta_tag %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="轻遇">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="实名制交友 App——每天只推荐几位，不刷屏，不信息过载">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# 登录后页面禁止 Turbo 快照缓存，避免显示过期状态（如认证等级） %>
    <% if authenticated? %>
      <meta name="turbo-cache-control" content="no-cache">
    <% end %>

    <%= yield :head %>

    <link rel="icon" href="/icon.svg" type="image/svg+xml">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-[#f7faf9] font-sans text-gray-800 antialiased" data-current-user-id="<%= current_user&.id %>">
    <%= render "shared/flash" %>
    <%= yield %>
  </body>
</html>
